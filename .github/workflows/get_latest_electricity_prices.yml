name: Get spot prices

on:
  workflow_dispatch:
  schedule:
    - cron: "30 * * * *"

jobs:
  get_spot_prices:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: list timezones
      run: timedatectl list-timezones
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.12"
    - name: Install deps
      run: pip install -r requirements.txt
    - name: Update SE1
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=SE1 --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update SE2
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=SE2 --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update SE3
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=SE3 --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update SE4
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=SE4 --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update DK1
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=DK1 --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update DK2
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=DK2 --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update NO1
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=NO1 --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update NO2
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=NO2 --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update NO3
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=NO3 --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update NO4
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=NO4 --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update NO5
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=NO5 --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update FI
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=FI --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }} 
    - name: Update DE_LU
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=DE_LU --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update AT
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=AT --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update FR
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=FR --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update BE
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=BE --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update NL
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=NL --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update PL
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=PL --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update EE
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=EE --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update LT
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=LT --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update LV
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=LV --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update IT-NORTH
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=IT-NORTH --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update IT-CENTRE_NORTH
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=IT-CENTRE_NORTH --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update IT-CENTRE_SOUTH
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=IT-CENTRE_SOUTH --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update IT-SOUTH
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=IT-SOUTH --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update IT-SICILY
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=IT-SICILY --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update IT-SARDINIA
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=IT-SARDINIA --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update IT-CALABRIA
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=IT-CALABRIA --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update CH
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=CH --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update ES
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=ES --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update PT
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=PT --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update SK
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=SK --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update SI
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=SI --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update CZ
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=CZ --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update HU
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=HU --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update HR
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=HR --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update RO
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=RO --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update RS
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=RS --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update BG
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=BG --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    - name: Update GR
      run:  python scripts/transparancy.py get-day-ahead-prices --price-area=GR --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}
    #- name: Update SEM
    #  run:  python scripts/transparancy.py get-day-ahead-prices --price-area=SEM --security-token=${{ secrets.TRANSPARENCY_PLATFORM_SECURITY_TOKEN }}

    - name: setup git config
      run: |
         # setup the username and email. 
         git config user.name "GitHub Actions Bot"
         git config user.email "<>"
    - name: stage changed files
      run: git add -A
    - name: detect changes
      id: git_status
      run: |
        if git diff --cached --quiet; then
          echo "changed=false" >> "$GITHUB_OUTPUT"
          echo "No changes detected."
        else
          echo "changed=true" >> "$GITHUB_OUTPUT"
          echo "Changes detected."
        fi
    - name: no changes
      if: steps.git_status.outputs.changed == 'false'
      run: echo "Nothing to commit; skipping git push."
    - name: pull latest
      if: steps.git_status.outputs.changed == 'true'
      run: git pull --rebase --autostash origin main
    - name: restage after rebase
      if: steps.git_status.outputs.changed == 'true'
      run: git add -A
    - name: commit changed files
      if: steps.git_status.outputs.changed == 'true'
      run: git commit -m "Added new prices"
    - name: push code to master
      if: steps.git_status.outputs.changed == 'true'
      run: git push origin main
